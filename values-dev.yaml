replicaCount: 1

image:
  repository: image-registry.openshift-image-registry.svc:5000/cicd-hector/httpd-demo-hector
  tag: "1.0.1-9e1a7e3"

# This is to override the chart name.
fullnameOverride: "prueba-hector"

service:
  type: ClusterIP
  port: 8080
route:
  host: ''
  weight: 100
  targetPort: 8080
  tls:
    enabled: true
    termination: edge
